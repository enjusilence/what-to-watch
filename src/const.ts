export enum AppRoute {
  SignIn = '/sign-in',
  MyList = '/my-list',
  Player = '/player/:id',
  Films = '/films/:id',
  AddReview = 'review',
  Root = '/',
}

export const TIMEOUT_SHOW_ERROR = 5000;
